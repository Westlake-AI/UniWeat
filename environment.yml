name: UniWeat
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - nni
  - numpy
  - hickle
  - opencv-python
  - pip
  - python
  - pytorch
  - xarray
  - pip:
    - fvcore
    - lpips
    - pickle
    - scikit-image
    - timm
    - tqdm
prefix: /opt/anaconda3/envs/uniweat